model Collaborator {
  id          String   @id @default(cuid())
  name        String
  organization String?

  // Collaborators may be linked to many projects, and projects to many collaborators (M:N)
  projects   Project[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
